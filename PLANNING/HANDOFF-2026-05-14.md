# Handoff — 2026-05-14

Documento para retomar trabalho em sessão futura. Lido com a memória
`project_estado_2026-05-14` carregada.

---

## Calendário até go-live

| Janela | Foco |
|---|---|
| **14-18/mai** | Fechar a ponte (manual + visualização + treinamento) + quick wins desbloqueadores |
| 19-31/mai | Service layer Onda 1+ (items 10, 18, 20) + fix gaps (TargetMarkets, validationStatus) + clonagem de cert |
| **jun (todo)** | **UI Onda 1+ U1-U11 — workflow completo de ponta a ponta nasce aqui** |
| 1ª-3ª sem jul | Smoke + treinamento operacional + ETL IFF-FAR |
| **30/jul** | **Go-live FAMBRAS** |

---

## Plano sprint 14-18/mai (essa semana, em ordem)

### Hoje 14/mai (tarde) — destrava treinamento

1. **🔴 Telas mínimas de visualização** (lista certs + lista empresas + detalhe)
   - **Por quê crítico:** menu analista hoje só tem "Emitir Certificado". Sem ver os 1023 mirror + 3 manual no banco, treinamento 18/mai fica capenga (analista escreve no escuro).
   - **Escopo MVP:**
     - Página `/certificados` — tabela paginada com `cert_number`, empresa, status, ciclo (% decorrido), action "Ver detalhe"
     - Página `/certificados/:id` — espelha o que `/verify` mostra ao público + dados internos (history, scope, links pra empresa/planta)
     - Página `/empresas` — tabela com legalName, taxId, country, group, plant count
     - Página `/empresas/:id` — empresa + lista plantas + lista certs ativos
     - Adicionar entries no Sidebar/MobileMenu para roles `analista`, `gestor`, `admin` (já adicionado em `gestor`, falta `analista`)
   - **Endpoints backend:** já existem (`GET /certificates`, `GET /certificates/:id`, `GET /companies`, `GET /companies/:id` — confirmar swagger e roles).
   - **Esforço estimado:** 1 dia.

2. **🔴 Investigar autocomplete BRF** (only 2 of 31 returned)
   - Pendente das queries que pedi ao PO (rodar `SELECT * FROM companies WHERE legal_name ILIKE '%BRF%'`).
   - Hipóteses: filtro `group_id` no service, LIMIT no autocomplete, legal_name divergente.

3. **🟡 Cleanup dos 3 certs órfãos** (codes lixo `C`/`G`/`CVCV`)
   - SQL via DBeaver: `DELETE FROM certificates WHERE issuance_mode='mirror' AND certification_id IN (SELECT id FROM certifications WHERE industrial_category_id IS NULL)` (em cascade limpa scope/history). 5 min.

### 15-17/mai — Onda 1+ antecipada + validação visual

| Item | Esforço | Roadmap |
|---|---|---|
| 🟢 Fix `TargetMarketsStep` chamar `bulkUpsertMarketScopes` | 2-3h | Gap pré-U1 |
| 🟢 Layout 2 páginas paisagem (extender `approval-renderer.ts`) | 4-6h | Lina entregou LUZ.docx em 13/mai |
| 🟢 `CertificationStandardByMarket` seed FM 4.1.X REV 03 (12 DTs × 13 mercados) | 4-6h | Auto-fill `applicableStandards` no manual-emit |
| 🟡 Alinhar `Company.validationStatus` (front × prisma) | 1-2h | Remove ruído |

**Validação visual com FAMBRAS:** PO emite 1 cert manual de cada DT principal e compara com PDFs reais (JBS/IRG/SEN/LUZ).

### 18/mai — treinamento

- 1h André + Fuad (presencial ou call). Roteiro:
  - Emissão manual (cobrir os 17 templates de selo)
  - Visualização (listas + detalhe — entregue dia 14)
  - QR Code + verify pública
  - Casos especiais (granel, álcool, multi-categoria)

---

## Onda 1+ — pegar a partir de 19/mai

### Service layer (3-4ª sem mai)

- **Item 10 — Hook `ScopeAmendment.onApprove()` → `CertificateService.reissue()`** para 5 dos 6 tipos. Trigger via Nest EventEmitter (não SQL).
- **Item 18 — `Proposal.feeOverride`** aceito sem fórmula + cálculo automático vira template sugerido sobrescrevível.
- **Item 20 — SLA reverso** 30d antes da auditoria + alertas D-30/D-15/D-7 (BullMQ + Redis).
- **Item 23 — Backfill `MarketDestination`** aditivo com `GulfRestrictionConfig`.

### UI Onda 1+ (jun) — **workflow completo**

| ID | Tela | Notas |
|---|---|---|
| U1 | Wizard solicitação 5 passos ramificado | JSON-driven, engine compartilhada com U3/U6 |
| U2 | Split-view analista FM 7.1.9 | Cálculo paramétrico GSO/SMIIC/IAF MD 5 |
| U3 | Hub alteração de escopo (5 cards) | 6 tipos de amendment |
| U4 | Engine wizard JSON-driven | Compartilhado |
| U5 | FM 9.3 unificado mobile | Seções colapsáveis carimbadas |
| U6 | Hub permanente IT 7.12 docs | Nudges contextuais |
| U7 | Editor planilha MP Airtable-like | Import Excel |
| U8 | Inbox analista | Fila FM 7.1.9 + HAS + MPs |
| U9 | Tela aditivo contratual + PDF preview | pdfjs-dist |
| U10 | Template proposta/contrato em PDF | pdfkit (já no stack) |
| U11 | Mercado interno × exportação dual-toggle | Chip colorido na listagem |

---

## Pendências de input externo

- **FM 4.1.X atualizada** (Qualidade FAMBRAS, sem data) — implementar REV 03 e plugar nova quando chegar.
- **Logomarcas industriais** (Lina) — hot-swap S3.
- **Tradução árabe** (Lina coordena) — `arabic-labels.ts`.
- **Domínio QR Code definitivo** — reunião técnica futura.

---

## Operacional (não-código, PO)

- ⏳ Criar usuários nominais analistas em prod + remover usuário `qualidade`
- ⏳ Recriar convite quinzenal segunda 11h com lembrete 30min
- ⏳ Validação visual com FAMBRAS 15-17/mai
- 📅 Treinamento 18/mai

---

## Estado dos repos (push 14/mai)

| Repo | Tip release | O que tem |
|---|---|---|
| `halalsphere-backend` | `f6766b2e` | verify expandida + multi-cat wire + dt-code fix + mirror script (Python) + migration mirror |
| `halalsphere-frontend` | `fafceaf7` | verify cards (planta/selos/cats/ciclo) + manual-emit form com multi-cat picker + sidebar/mobile reorg roles |
| `halalsphere-docs` | `a167891` | anexos FAMBRAS 13/mai + 2 docs SQL (mirror migration + espelhamento) |

Espelhamento em prod (queries de validação OK):
- 1023 certs (mirror) + 3 manuais
- 562 companies + 575 plants no group `99c089ed-38db-52a1-8271-e2d575ab84e7`
- Distribuição multi-cat: 907 com 1 cat, 107 com 2, 6 com 3

---

## Para retomar — passo concreto

1. Carregar memória `project_estado_2026-05-14`.
2. Confirmar prioridade: **telas de visualização (item 🔴1 acima)** é o próximo passo concreto.
3. Antes de codar, perguntar ao PO se ele rodou a query de diag do BRF — se sim, ajustar autocomplete junto.
4. Estimar 1 dia para telas mínimas; PO valida em staging antes de pushar release.
